apiVersion: v1
kind: Service
metadata:
  name: {{ include "cqrs.fullname" . }}
  labels:
    {{- include "cqrs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cqrs.selectorLabels" . | nindent 4 }}
